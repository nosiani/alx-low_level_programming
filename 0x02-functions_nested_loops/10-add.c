#include "main.h"

/**
 * add -This program adds two integers and returns the result
 * a: first parameter to be added to b
 * b: second parameter to be added to a
 * Return: sum of a and b
 */
int add(int a, int b)
{
	int c;

	c = a + b;

	return (c);
}
