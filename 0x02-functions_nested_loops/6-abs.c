#include "main.h"

/**
 * _abs - This program displays the absolute value of an integer
 *
 * Return: the absolute value of the integer
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
